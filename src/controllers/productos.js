const {Productos, Precio, Desafios} = require('../db'); //fijarnos el nombre con que lo pone pablo.
const axios = require ("axios");

function productos(req,res,next){

    const nombre_input = req.params.nombre;


}

module.exports = {productos};